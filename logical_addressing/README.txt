Problems: the last context switch (after the first program finishes) doesn't execute properly, resulting in the first mult.asm's result storing itself on top of the second mult.asm's result.  :(

Everything else should work fine. 
